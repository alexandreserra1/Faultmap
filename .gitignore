# Artefatos criados por faultmap init na máquina local.
faultmap.db
faultmap.db-shm
faultmap.db-wal
faultmap-out/
faultmap.yaml

# Saídas locais de ferramentas Go.
coverage.out

# Grafo de conhecimento: regenerável por `graphify update`; o cache e o HTML
# são grandes e mudam a cada execução.
graphify-out/cache/
graphify-out/graph.html
